---
description:
globs:
alwaysApply: true
---
# Vue Naive Admin 组件系统

## 组件目录

- 公共组件：[src/components/](mdc:src/components) - 可复用的通用组件
- 页面组件：[src/views/](mdc:src/views) - 与路由对应的页面组件
- 布局组件：[src/layouts/](mdc:src/layouts) - 页面布局组件

## 组件自动导入

项目使用 [unplugin-vue-components](mdc:https:/github.com/antfu/unplugin-vue-components) 实现组件自动导入：

- Naive UI 组件自动导入
- 项目自定义组件自动导入

## 组件命名规范

- 公共组件：使用 PascalCase 命名，例如 `BaseButton.vue`
- 页面组件：使用 PascalCase 命名，例如 `UserProfile.vue`
- 单例组件：以 `The` 前缀命名，例如 `TheHeader.vue`

## 组件编写规范

- 使用 `<script setup>` 语法
- 使用 TypeScript 进行类型定义
- 使用 Props 接口定义组件属性
- 使用 Emits 定义组件事件
- 使用组合式 API 组织组件逻辑

